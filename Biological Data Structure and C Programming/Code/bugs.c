#include <stdio.h>;
/*
int main (void)
{
     int x = 1;
     char a = 'a';
     float 1point1 = 1.1;

     printf("An integer: %i\n, x);
     printf("A character: %i\n", a)
     printf("A floating point: %f\n", 1point1);

     return 0;
]
-has a curly brace
-printf in a is for integer (%i) instead of character (%c)
-missing quotation marks
-bad variable naming starting with an integer
*/

int main (void)
{
     int x = 1;
     char a = 'a';
     float point_1 = 1.1;

     printf("An integer:" %i\n, x);
     printf("A character:" %c\n", a)
     printf("A floating point:" %f\n", 1point1);

     return 0;
}
