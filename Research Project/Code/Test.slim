// set up a simple neutral simulation
// initialise one chromosome 
initialize() {
	initializeMutationRate(1e-7);
	
	// m1 mutation type: neutral
	initializeMutationType("m1", 0.5, "f", 0.0);
	
	// g1 genomic element type: uses m1 for all mutations
	//represents particular chromosomal region
	initializeGenomicElementType("g1", m1,1.0);

	// uniform chromosome of length 100 kb- for loop to create three locus
	//for (i in 1:3)
	//initializeGenomicElement(g1, i*1000, i*1000 + 499);
	initializeGenomicElement(g1, 1, 99999);
	
	initializeRecombinationRate(1e-8);
}

// create a population of 500 individuals
1 {
	sim.addSubpop("p1", 500);
}

// output samples of 10 genomes periodically, all fixed mutations at end
1000 late(){ 
		 sim.outputFixedMutations();
		 sim.simulationFinished(); 
	  }

